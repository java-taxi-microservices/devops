apiVersion: v1
kind: Secret

metadata:
  name: registration-orchestrator-secret

type: Opaque

stringData:
  POSTGRES_USER: "{{ .Values.secret.postgresUser }}"
  POSTGRES_PASSWORD: "{{ .Values.secret.postgresPassword }}"
  POSTGRES_URL: "{{ .Values.secret.postgresUrl }}"
